#include "main.h"

/**
 * main - test for the fuction that prints if integer is negative or positive
 *
 * Return: always 0
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
