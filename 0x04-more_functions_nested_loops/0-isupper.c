#include "main.h"

/**
 *  _isupper - check uppercase character
 * @s: number to be checked
 * Return: 1 for uppercase or 0 if otherwise
 */
int _isupper(int s)
{
	if (s >= 65 && s <= 90)
	{
		return (1);
	}
	return (0);

}
