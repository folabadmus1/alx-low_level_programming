#include "main.h"

/**
 * mul - multiplies two integers
 *
 * @s: the first number to be multiply
 *
 * @j: the second number to be multiply with second number.
 *
 * Return: the result of the multiplication the s parameter and j parameter
 */
int mul(int s, int j)
{
	return (s * j);
}
