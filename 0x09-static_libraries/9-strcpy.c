#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strcpy - A main function.
 *
 * @dest: the string to print
 *
 * @src: the second string to be printed
 *
 * Return: Always 0 (success)
 */

char *_strcpy(char *dest, char *src)

{
	return (strcpy(dest, src));
}
