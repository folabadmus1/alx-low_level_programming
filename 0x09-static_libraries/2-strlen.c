#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strlen - A main function.
 *
 * @s: the string to print
 *
 * Return: Always 0 (success)
 */
int _strlen(char *s)
{
	return (strlen(s));
}
