#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strcat - A main function.
 *
 * @dest: the string to print
 *
 * @src: the second string to be printed
 *
 * Return: Always 0 (success)
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
