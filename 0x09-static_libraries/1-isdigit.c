#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isdigit - A main function.
 *
 * @c: the string to print
 *
 * Return: Always 0 (success)
 */

int _isdigit(int c)
{
	return (isdigit(c));
}
