#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strcmp - A main function.
 *
 * @s1: the string to print.
 *
 * @s2: the second string to be printed
 *
 * Return: Always 0 (success)
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
